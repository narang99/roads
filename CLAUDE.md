# Claude Development Notes

## Python Toolkit

Use `uv run` inside the `mtrain` directory to execute Python scripts and commands. This ensures the correct environment and dependencies are used.

Example:
```bash
cd mtrain
uv run python scripts/simplify_crop_level.py --help
```