# Interpretation

I'm trying to map the journy an image takes through a convolutional neural network.  
The final goal is to be able to tweak the network in desired ways without having a lot of annotated data.  

- I currently have the weights and activations stored for that model
  - We have code to go through them one by one along with layer type
- The first thing I can do is visualise the activations at each stage
- Then we get a copy of the input of the weight, the output of the weight, and the weight itself.  
  - Analyse every operation
- Analyse every convolution to see its effects on other images. I need to end up with some "test" images which tell me what is the intended effect of the convolution


## Problems

- the most obvious problem is that resnets don't really have 2D convolutions. These are 3D convolutions. For input of size `[4,65,65]`, the layer would have convolution kernels of size `[4,k,k]`. Each kernel takes `k x k` patch on each channel and sums across all channels. You can have `n` kernels, and they would then give `n` outputs. 
- Each output is a brand new image
  - For the case of 2D convolutions that we do on a grayscale


- Looking at convolution outputs at the top level by seeing  my own input image and all convolution outputs is not very useful
  - I need to see all input channels separetely.
  - See each output channel with respect to them
  - See how to kernel behaves
- The definition of convolution which associates each number in the kernel as the "probability" of the pixel that number is targeting seems to be the easiest to use in 3 dimensions
  - A 3d kernel of size (3,k,k) would basically assign probability to each input (it can be negative, it can be positive)
  - I need to color the pointwise multiplication before the color for each pixel
    - Now we only have each pixel with this, we then would also like to see how the pattern looks for the next pixel
    - and so on
    - It would be useful to see how I can visualize sobel before going to 3D


- We first need to find this sort of mapping, which part of input affects which part of output in our k channel convolution. Then we need to start backwards, from the end of the network, we know what the target is (in segmentation, what is the target?). Then we see where the source comes from for that target, then we keep going backwards. Might be what gradcam is doing. 
  - I'm however interested in how the network works finally
  - Which convolutions are doing what at a macro level (kinda like finding what kind of operation they are doing)
  - Then we assign each kernel a simple "type" (like resize). With some parameters (like how much?). This can also be something "pattern recogniser" which activates for very specific patterns (like a "pigeon" shape).  


# First goal
- Map how a single convolution layer is working (each kernel)
  - I don't need to get the raw model definition here atleast. I can keep working with fastai's defined model. It has enough granularity

## Sobel
- Start with sobel, but why lol, I forgot xD
  - I want to visualise point wise multiplication for a single convolution in the 2D image
  - I also want to see the pattern. The simple pointwise multiplication in the end only results in a single pixel
    - But it creates a trend for the input. We want to see how the trend for individual pixels look like while the filter is passing through inputs
  - This is a lot harder to reason about than I thought. 
    - For one thing, just seeing the kernel hardly gives me a pattern

## Set of images

- Need a set of useful images to see how a kernel reacts to an image
  - images of some color (although we are doing per channel)

# Flowers tackle

The first problem is having many flowers getting detected.  
I can train a model which downloads flowers online, shrinks them, along with taco and random crops from already present mapillary data   
I will also make the model get some elevvated vegetation samples in negatives too (this helps it distinguish when some of elevated vegeration passes the segmentation model).  

- Flowers from inaturalist (1 part)

Non flower category:
- TACO (45%)
- random crops from mapillary samples and dehi data (45%)
- random crops from elevated vegetation (10%)

We create twice the amount of data and done

# Too many others

The problem of detecting too many others still remains. The model has good recall on trash (if it says trash, it is trash many times). But I still feel the number of others is ridiculous. I'm not happy with the confusion matrix right now.  


## Blur everything other than ROI

We blur stuff other than region of interest. This is also giving an okay ish performance for now.  
Its not bad, not that good also. The reasoning was that the model would focus on the unblurred part, use the blurred part as extra context.  

Two important factors:
- blur kernel size and sigma
  - I've tried kernel-size=3, padding=5
- It might be useful to increase the blur (the model only needs contextual information of where it "is"). Higher blur might be helpful. Higher padding would also be good?

How do I do the distinction in padding?

## pretrained backbone

- the 4chan model gave reasonable performance. I'm assuming it focused on the padded input quite a lot to provide that performance.  (tight pad was 20 pixels wide).  
  - Current pad is quite small (only 5 pixels wide). I might benefit from increased padding

- So first, we train the model on only padded data. The crop would be 130x130, the center would be cropped out from the image at some configurable padding (5,10,20). this would then put the data on a blank black pixel image of 130x130 size.  
  - I should look at the data myself too.
  - whichever data the model performs well in wins.
- I dont want the model to try too hard. Using std cross entopy loss is fine
 

### what is the problem with higher padding?
Higher padding increases the number of objects in scene. The model can see another patch of garbage in the scene and decide that the current patch is garbage.   

Lets look at the generated data I guess.  
